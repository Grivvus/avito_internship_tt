FROM golang:1.25 AS builder

WORKDIR /app

RUN go install github.com/pressly/goose/v3/cmd/goose@latest

COPY .env ./.env
COPY migrations/ ./migrations/

SHELL ["goose -env=.env -dir=/app/migrations/ up"]
